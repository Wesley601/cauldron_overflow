{% extends "base.html.twig" %}
{% block body %}
    <h1>{{ question }}</h1>

    <div>
        Eventily, we'll print the full question here!
    </div>

    <h2>answers ({{ answers|length }})</h2>

    <ul>
        {% for answer in answers %}
            <li>{{ answer }}</li>
        {% endfor %}
    </ul>
{% endblock %}
